Libraries required for the Streamlit app

streamlit==1.30.0
python-dotenv
nest-asyncio

RAG/PDF Processing Libraries (Downgraded for max stability against pydantic conflicts)

pypdf
langchain==0.1.0 # Set the main LangChain package to a highly stable, early version
langchain-core==0.1.1 # Ensures core utilities are compatible with the old main package
langchain-community==0.0.12 # Stable version of community integrations
langchain-google-genai==0.0.1 # Very stable, early version of the Google integration
faiss-cpu==1.8.0
google-genai